# .env file (DO NOT COMMIT TO GIT - This file is for LOCAL development settings)

# SECURITY WARNING: Make this unique and keep it secret in production!
# For local development, a simple key is often sufficient.
SECRET_KEY='django-insecure-local-dev-key-replace-if-needed'

# SECURITY WARNING: Never run with debug turned on in production!
DEBUG=True

# Hosts allowed for local development server
ALLOWED_HOSTS=127.0.0.1,localhost

# Database URL (if different from default SQLite, otherwise DATABASES in settings.py handles it)
# DATABASE_URL=sqlite:///db.sqlite3

# Email backend for local testing (outputs emails to console instead of sending)
EMAIL_BACKEND='django.core.mail.backends.console.EmailBackend'

# Optional: If you want to test actual email sending locally (less recommended)
# Requires setting up an email provider (like Gmail with App Password)
# EMAIL_HOST='smtp.gmail.com'
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER='your_local_test_email@gmail.com'
# EMAIL_HOST_PASSWORD='your_local_app_password'

# Email address for receiving contact form submissions during local testing
CONTACT_FORM_RECIPIENT_EMAIL='test@example.com'
